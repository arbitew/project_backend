<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/main_page.css">
    <title>{{ title }}</title>
</head>
<body>
    <div class="container">
        <div class="bar">
            <a href="/sign_in"> <img class = "icons" src="images/icons8-exit-100.png" width="70px"> </a>
            
        </div>
            <div class="character_box">
                <div class = "tasText">TASKS</div>
                {{!-- <div>Name:{{name}}</div>
                <div>ID:{{id}}</div> --}}
                <div style="font-size: 20px;">List of group:</div>
                <form action="/profile" method="POST">

                        <input name="name" value={{name}} type="hidden">
                        <input name="password" value={{password}} type="hidden">
                        <button class="btn-hover color-7" type="submit">Profile</button>

                </form>
                <form action="/main_page" method="POST">

                        <input id = "groupInput"name="new_group" placeholder="Enter name of group">
                        <input name="id" value={{id}} type="hidden">
                        <input name="name" value={{name}} type="hidden">
                        <input name="password" value={{password}} type="hidden">
                        <button class="btn-hover color-7" type="submit">Create</button>

                </form>
                {{#if all_groups.length}}

                {{#each all_groups}}
                {{!-- show each group from db --}}
                <li class="todo">
                    <form action="/go_group" method="POST">
                            <button class="btn-hover color-7">{{group_name}}</button>
                            <input type="hidden" value="{{_id}}" name="id_group">
                            <input type="hidden" name="name" value={{../name}}>
                            <input type="hidden" name="password" value={{../password}}>
                    </form>
                </li>
                {{/each}}

                {{else}}
                <p>No groups!</p>
                {{/if}}
            </div>
        </div>

</body>

</html>